# Proxy probe fixtures

These minimized snapshots contain only model discovery and empirical acceptance fields used by the probe tests.
Secrets, request headers, access groups, and real backend hosts are intentionally excluded.
