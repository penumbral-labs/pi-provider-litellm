The Messages acceptance fixture records the 2026-09-04 proxy probe run: HTTP 200 means accepted, while HTTP 400
`UnsupportedParamsError` means rejected. The accepted `claude-fable-5-1` row uses the same-generation
`claude-fable-5` Messages serializer contract because the pinned Pi catalog does not contain the exact minor id.
